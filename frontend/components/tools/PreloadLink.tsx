import React from 'react
interface PreloadLinkProps {
  // 定义组件属性
}
const PreloadLink: React.FC<PreloadLinkProps> = (props) => {
  return (
    <div className="preloadlink">
      <h1>PreloadLink</h1>
      <p>组件内容</p>
    </div>
  );
};
export default PreloadLink;
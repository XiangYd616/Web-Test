import React from 'react

interface StressTestHistoryProps {
  // 定义组件属性
}

const StressTestHistory: React.FC<StressTestHistoryProps> = (props) => {
  return (
    <div className="stresstesthistory">
      <h1>StressTestHistory</h1>
      <p>组件内容</p>
    </div>
  );
};

export default StressTestHistory;

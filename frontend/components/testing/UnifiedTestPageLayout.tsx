/**
 * 统一测试页面布局组件
 * 这是 TestPageLayout 的别名，用于向后兼容
 */

import { TestPageLayout } from './TestPageLayout';

// 直接导出 TestPageLayout 作为默认导出
export default TestPageLayout;

// 也导出命名导出以支持不同的导入方式
export { TestPageLayout as UnifiedTestPageLayout };

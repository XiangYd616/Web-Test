import React from 'react

interface TestResultComparisonProps {
  // 定义组件属性
}

const TestResultComparison: React.FC<TestResultComparisonProps> = (props) => {
  return (
    <div className="testresultcomparison">
      <h1>TestResultComparison</h1>
      <p>组件内容</p>
    </div>
  );
};

export default TestResultComparison;

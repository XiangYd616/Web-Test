import React from 'react

interface StressTestDetailModalProps {
  // 定义组件属性
}

const StressTestDetailModal: React.FC<StressTestDetailModalProps> = (props) => {
  return (
    <div className="stresstestdetailmodal">
      <h1>StressTestDetailModal</h1>
      <p>组件内容</p>
    </div>
  );
};

export default StressTestDetailModal;

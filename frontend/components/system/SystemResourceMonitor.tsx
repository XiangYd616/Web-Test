import React from 'react

interface SystemResourceMonitorProps {
  // 定义组件属性
}

const SystemResourceMonitor: React.FC<SystemResourceMonitorProps> = (props) => {
  return (
    <div className="systemresourcemonitor">
      <h1>SystemResourceMonitor</h1>
      <p>组件内容</p>
    </div>
  );
};

export default SystemResourceMonitor;

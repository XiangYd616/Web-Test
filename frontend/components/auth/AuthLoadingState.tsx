import React from 'react
interface AuthLoadingStateProps {
  // 定义组件属性
}
const AuthLoadingState: React.FC<AuthLoadingStateProps> = (props) => {
  return (;
    <div className="authloadingstate">;
      <h1>AuthLoadingState</h1>
      <p>组件内容</p>
    </div>
  );
};
export default AuthLoadingState;
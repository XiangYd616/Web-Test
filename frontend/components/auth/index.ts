// index - 自动生成的基础模板
export interface indexOptions {
  // 定义选项接口
}
export class index { private options: indexOptions;
  constructor(options: indexOptions = { }) {
    this.options = options;
  }
  // 添加方法实现
}
export default index;
import React from 'react
interface withAuthCheckProps {
  // 定义组件属性
}
const withAuthCheck: React.FC<withAuthCheckProps> = (props) => {
  return (;
    <div className="withauthcheck">;
      <h1>withAuthCheck</h1>
      <p>组件内容</p>
    </div>
  );
};'"
export default withAuthCheck;'"
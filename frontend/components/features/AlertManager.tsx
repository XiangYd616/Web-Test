import React from 'react
interface AlertManagerProps {
  // 定义组件属性
}
const AlertManager: React.FC<AlertManagerProps> = (props) => {
  return (;
    <div className="alertmanager">;
      <h1>AlertManager</h1>
      <p>组件内容</p>
    </div>
  );
};
export default AlertManager;
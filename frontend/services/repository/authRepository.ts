/**
 * 认证数据访问层(兼容旧路径)
 * 请统一使用 services/api/repositories/authRepository
 */

export * from '../api/repositories/authRepository';
export { authRepository, default } from '../api/repositories/authRepository';

/**
 * 用户数据访问层(兼容旧路径)
 * 请统一使用 services/api/repositories/userRepository
 */

export * from '../api/repositories/userRepository';
export { default, userRepository } from '../api/repositories/userRepository';

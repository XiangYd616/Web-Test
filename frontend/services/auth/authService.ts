// authService - 自动生成的基础模板

export interface authServiceOptions {
  // 定义选项接口
}

export class authService {
  private options: authServiceOptions;

  constructor(options: authServiceOptions = {}) {
    this.options = options;
  }

  // 添加方法实现
}

export default authService;

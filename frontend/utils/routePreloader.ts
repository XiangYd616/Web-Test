// routePreloader - 自动生成的基础模板

export interface routePreloaderOptions {
  // 定义选项接口
}

export class routePreloader {
  private options: routePreloaderOptions;

  constructor(options: routePreloaderOptions = {}) {
    this.options = options;
  }

  // 添加方法实现
}

export default routePreloader;

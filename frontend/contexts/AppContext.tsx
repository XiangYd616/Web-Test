// AppContext - 自动生成的基础模板

export interface AppContextOptions {
  // 定义选项接口
}

export class AppContext {
  private options: AppContextOptions;

  constructor(options: AppContextOptions = {}) {
    this.options = options;
  }

  // 添加方法实现
}

export default AppContext;

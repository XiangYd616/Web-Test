# npm 配置文件
# 确保使用 package-lock.json
package-lock=true

# 在安装时保存精确版本
save-exact=false
save-prefix=^

# 工作区配置
workspaces=true

# 日志级别
loglevel=warn

# 安全审计级别
audit-level=moderate

# 自动修复漏洞
# audit-fix=false

# 进度显示
progress=true

# 引擎严格检查（可选 - 如果 Node 版本不匹配会报错）
# engine-strict=true

# 使用国内镜像（可选 - 如果需要加速）
# registry=https://registry.npmmirror.com

